services:
  hello:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hello
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SERVER_PORT: 8080
